import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'app-shop',
  template: `Items works!`,
  styleUrls: ['./items.component.scss'],
})
export default class ItemsComponent {}
